const SWAP_CATEGORY = 'swap'

export const SWAP_EVENTS = {
  SWAP_ASSETS: {
    action: 'Swap asset',
    category: SWAP_CATEGORY,
  },
  SWAP_TOKENS: {
    action: 'Swap total asset',
    category: SWAP_CATEGORY,
  },
}
