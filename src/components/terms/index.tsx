import { Typography } from '@mui/material'

const SafeTerms = () => {
  return (
    <div>
      <Typography variant="h1" mb={2}>
        Terms
      </Typography>
    </div>
  )
}

export default SafeTerms
